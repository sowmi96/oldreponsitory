import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { KntSchemeComponent } from './knt-scheme.component';

describe('KntSchemeComponent', () => {
  let component: KntSchemeComponent;
  let fixture: ComponentFixture<KntSchemeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ KntSchemeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(KntSchemeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
