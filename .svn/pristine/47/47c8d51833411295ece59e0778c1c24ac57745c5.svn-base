import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MadeOrderLoginComponent } from './made-order-login.component';

describe('MadeOrderLoginComponent', () => {
  let component: MadeOrderLoginComponent;
  let fixture: ComponentFixture<MadeOrderLoginComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MadeOrderLoginComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MadeOrderLoginComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
